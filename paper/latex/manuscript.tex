\documentclass[pdflatex,sn-basic,Numbered,iicol]{sn-jnl}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{algorithm,algpseudocode}
\usepackage{graphicx,amsmath,amssymb,booktabs,multirow,placeins,xurl}
\hypersetup{hypertexnames=false}
\ifdefined\pdfminorversion\pdfminorversion=7\fi
\raggedbottom
\makeatletter
\setlength{\@dblfptop}{0pt}
\setlength{\@dblfpsep}{12pt}
\setlength{\@dblfpbot}{0pt plus 1fil}
\setlength{\@fptop}{0pt}
\setlength{\@fpsep}{12pt}
\setlength{\@fpbot}{0pt plus 1fil}
\newcommand{\reviewtablecaption}{\def\@captype{table}\caption}
\newcommand{\reviewfigurecaption}{\def\@captype{figure}\caption}
\makeatother
\emergencystretch=2em
\begin{document}
\title[Emulating conformal reference sets]{Label-efficient emulation of conformal reference sets for wearable activity recognition}
\author[1]{\fnm{Xin} \sur{Feng}}
\author*[1]{\fnm{Decong} \sur{Yang}}\email{20232501525@stu.xju.edu.cn}
\affil[1]{\orgname{Xinjiang University}, \orgaddress{\city{Urumqi}, \country{China}}}
\abstract{Labeling every window in a calibration pool can be costly for wearable activity recognition. We propose joint rank calibration (JRC) to reduce this effort while preserving the prediction sets defined by the complete pool. A coupled rank-state recursion calibrates both quantile boundaries across the declared query checkpoints. Labels are acquired in uniform random order without replacement, and acquisition stops when the endpoint prediction sets differ by at most a specified mean number of labels on an unlabeled monitoring batch. The resulting confidence event guarantees reference-set containment for every input and controls average set inflation on that batch. The main evaluation uses seven public datasets and 354 classifier cases, including a separately locked confirmation. At confidence error 0.05 and tolerance 0.5, JRC saves 23.67\% of reference labels on the confirmation dataset, compared with 17.91\% for Bonferroni hypergeometric intervals and 11.74\% for the primary uniform-prior confidence sequence. A subsequent prior diagnostic improves that baseline to 15.33\%. An additional, separately specified nine-user confirmation contributes 45 cases and yields 34.03\% savings for JRC versus 25.13\% for the stronger-prior baseline. The results identify when joint rank calibration provides useful savings and how the benefit depends on reference-set quality.}
\keywords{Human activity recognition, Conformal prediction, Label acquisition, Finite-population inference, Sequential calibration}
\maketitle
\section{Introduction}
Wearable human activity recognition (HAR) systems often encounter users whose movements differ from the training cohort. Under this shift, predicted class probabilities can become poorly calibrated \cite{guo,ovadia}. Prediction sets represent uncertainty by retaining several plausible activities, but constructing a calibration reference requires labeled sensor windows. Reducing this labeling effort is especially useful when a reference pool contains many repeated measurements from several users.

Conformal prediction constructs prediction sets under specified calibration assumptions \cite{vovk,angelopoulos}. Hierarchical and beyond-exchangeability methods address dependencies and distribution shifts through additional structure \cite{dunn,barber}. We focus on a complementary acquisition problem: given a frozen classifier and a fixed finite pool, how many labels are needed to preserve the prediction sets defined by labeling the whole pool? We seek an output that contains those reference sets while allowing a controlled increase in their average size on an unlabeled monitoring batch.

We propose joint rank calibration (JRC) for this purpose. Labels are queried in uniformly random order without replacement. A coupled state records how many sampled ranks lie below the reference rank and whether the reference item has appeared. Its recursion computes the probability that either quantile boundary is crossed at any declared checkpoint. This joint calculation allocates the confidence budget across the full rank path. Acquisition stops when the prediction sets at the two running endpoints are sufficiently close on the monitoring batch, and the upper endpoint defines the returned predictor.

Sequential calibration and prediction-set-based stopping build on established work. Ilyas et al. study labeling and set-size costs under independent sampling \cite{ilyas}, while confidence sequences support inference at data-dependent stopping times \cite{howard2021,howard2022,wor}. JRC uses these foundations to control preservation of a fixed finite reference. Its specific contribution is the coupled rank-path calibration and its integration with a stopping rule expressed directly in prediction-set size.

The study makes three contributions:
\begin{enumerate}
\item A finite-reference acquisition formulation that connects high-probability set containment with an interpretable mean set-inflation tolerance.
\item A joint rank-state recursion that calibrates both quantile boundaries over the declared checkpoints and supports label-free evaluation of the stopping criterion.
\item An evaluation of 354 classifier cases on seven public datasets, followed by 45 additional cases in a separately specified confirmation cohort. Comparisons examine interval methods, acquisition controls, mechanism ablations and stronger confidence-sequence priors.
\end{enumerate}
The classifiers are logistic regression (LR), MiniRocket (MR) and an InceptionTime-style (IT) network. Six datasets support development, USC-HAD provides the main confirmation, and the additional cohort tests the unchanged method. The analysis relates label savings to reference-set quality and computational cost.

\section{Related work}
\subsection{Wearable recognition and uncertainty}
Recent HAR frameworks improve representation learning and adaptation. STF-AL combines temporal, spatial and frequency representations with adversarial domain adaptation \cite{stfal}. CCT-HAR uses cross-modal sample mining and temporal alignment \cite{ccthar}, and TSDFSA studies attention-based feature selection for multiple sensors \cite{tsdfsa}. These methods motivate controlled evaluation of each component in a recognition pipeline. Our comparisons hold the trained classifier fixed to isolate the effect of calibration-label acquisition.

Uncertainty estimation is also important in HAR. Roy et al. study confidence-calibrated recognition \cite{roy}, and hyperdimensional computing combined with conformal prediction supports uncertainty-aware inference on microcontrollers \cite{hduq}. MiniRocket \cite{minirocket} and InceptionTime \cite{inception} provide established model families with contrasting representations. We use them to examine how acquisition behavior changes with the underlying score distribution.

\subsection{Label-efficient stopping and its statistical target}
Ilyas et al. \cite{ilyas} study a stream of $n$ independent and identically distributed (i.i.d.) examples and the cost
\begin{equation}
 C(m)=m+(n-m)\,\mathbb E[|T_m(X)|]/K.
 \label{eq:relatedcost}
\end{equation}
Their procedure estimates normalized set size from candidate-label scores on unlabeled inputs and stops using a lower confidence bound on the cost increment. The regret analysis uses a monotone score-density-ratio assumption. The original-submission manuscript also describes a finite-sample stabilization for the experiments. These ideas motivate stopping decisions based on the observable size of prediction sets.

JRC targets containment of the prediction sets defined by a fixed reference pool. Its confidence statement is conditional on that pool and the uniform query order, with an additional size constraint on the monitoring batch. Table~\ref{tab:related} summarizes the statistical targets of the related approaches. The Ilyas comparison follows the original-submission version and distinguishes its theoretical stopping rule from the experimental stabilization.

Training-conditional validity has a longer history \cite{vovk2012}. Liang et al. study conformal inference with early stopping of neural-network training and reuse of hold-out data \cite{liangstop}. Xu et al. develop active, anytime-valid risk-controlling sets using a labeling policy and risk-predictor assistance \cite{active2024}. The August 2026 preprint by Skalse et al., \emph{Online Conformal Prediction Beyond Feedback} (arXiv:2608.07139v1), studies a game with prediction and label-query actions on non-independent streams. Together, these studies address limited labeling resources through several population-risk and sequential-decision objectives.


\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{4pt}
\caption{Related statistical targets and acquisition decisions}\label{tab:related}
\begin{tabular}{p{24mm}p{38mm}p{42mm}p{44mm}}
\toprule
Procedure & Sampling target & Stopping or acquisition decision & Guarantee considered \\
\midrule
Ilyas et al.\ \cite{ilyas} & I.i.d. data-generating distribution & Cost-increment rule; empirical set size estimated from unlabeled inputs & Population coverage; asymptotic cost regret under a density-ratio condition \\
Howard and Ramdas\ \cite{howard2022} & Population quantiles from an i.i.d. stream & Any stopping time supported by the confidence sequence & Time-uniform confidence intervals for quantiles and distribution functions \\
Waudby-Smith and Ramdas\ \cite{wor} & Fixed population sampled without replacement & User-selected stopping applied to a confidence sequence & Time-uniform confidence sets for finite-population parameters \\
JRC & Corrected order statistic of a fixed finite reference pool & Endpoint set-size difference below a batch-specific tolerance & Reference-set containment for all inputs; average inflation control on the monitoring batch \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize The rows summarize the statistical targets. JRC evaluates acquisition rules under a common finite-reference containment requirement and monitoring-batch size tolerance\end{minipage}
\end{table*}

\subsection{Sequential finite-population inference}
Finite-population sampling and hypergeometric inversion are standard statistical tools \cite{cochran}. Time-uniform confidence sequences \cite{howard2021}, sequential quantile estimation \cite{howard2022}, sampling-without-replacement confidence sequences \cite{wor}, and exact hypergeometric group-sequential tests \cite{jin} supply the closest foundations. The corrected conformal quantile and cumulative-probability scores are likewise established \cite{angelopoulos,romano}. JRC contributes a specialized joint rank-state calculation for a known finite horizon and reference rank. The experiments isolate the benefit of this calculation by applying the same reference pool, query order and set-inflation tolerance to established interval methods.

\section{Finite-pool reference emulation}
\label{sec:method}

Figure~\ref{fig:1} shows the frozen-model and query workflow. Algorithm~\ref{alg:jrc} specifies the acquisition procedure.

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig1.pdf}}
\caption{Joint rank calibration workflow. Panel (a) defines the frozen classifier and available inputs; panel (b) constructs the rank boundaries before querying; panel (c) acquires labels and applies prediction-set stopping. Running maxima and minima use the visited checkpoints, and both sums range over the declared monitoring batch. The checkpoint set in panel (b) is $\mathcal T$; $T$ denotes the stopping query count}\label{fig:1}
\end{figure*}

\subsection{Target, information and cost}
Let a trained classifier be fixed before calibration acquisition. For a nonconformity score $s(x,y)$, its thresholded prediction set is
\begin{equation}
 C(x;q)=\{y\in\mathcal Y:s(x,y)\le q\}.
 \label{eq:sets}
\end{equation}
The label set $\mathcal Y$ is finite, with $K$ classes. We fix a reference pool $\mathcal P=\{x_i\}_{i=1}^N$ before requesting its labels. The unknown true-label scores $S_i=s(x_i,y_i)$ are regarded as fixed values. Define
\begin{equation}
 k=\lceil(N+1)(1-\alpha)\rceil,\qquad
 q_{\rm ref}=S_{(k)},
 \label{eq:reference}
\end{equation}
with $q_{\rm ref}=+\infty$ if $k>N$. The full-pool reference predictor is $C(x;q_{\rm ref})$. Our target is to emulate this predictor with fewer queried labels. The reference rank uses the familiar split-conformal correction, but dependence within a pool does not make the resulting predictor marginally valid. Reference containment and population coverage are different statements.

We condition on all pool values, the trained classifier and a batch $\mathcal X_\star$ of $M$ unlabeled monitoring inputs. The probability statements concern a uniform permutation of the $N$ pool indices, selected independently of their unrevealed labels. This design accommodates dependencies among people, repeated windows and trials because inference conditions on the complete finite pool.

All class scores on $\mathcal X_\star$ are available without its labels. They determine when a quantile interval is sufficiently precise for the intended prediction sets. The size-control procedure is transductive: the monitoring inputs are available during acquisition, and the average inflation bound applies to that batch. Reference-set containment holds for every input on the confidence event.

The primary cost is the number $T$ of queried window labels. When the pool involves $m$ participants and each has setup cost $c$, a conservative secondary effort model is $T+cm$, against the reference cost $N+cm$. Every participant's setup is charged to both policies, even if stopping occurs before that participant's first queried window. These are modeled effort units. Presegmentation, obtaining sensor recordings and constructing the reference pool are outside the label-query savings.


\begin{algorithm*}[t]
\caption{Joint rank calibration for a fixed reference pool}\label{alg:jrc}
\begin{algorithmic}[1]
\Require Frozen score function; fixed pool of $N$ windows; uniform query permutation; monitoring scores; $\alpha,\delta,\tau$; checkpoints $\mathcal T$
\State Set $k=\lceil(N+1)(1-\alpha)\rceil$ and calibrate rank boundaries $(\ell_t,u_t)$ by the joint recursion
\Statex \hspace{\algorithmicindent}\textit{Joint recursion: Section~\ref{sec:joint}, Eqs.~\eqref{eq:transition}--\eqref{eq:crossing}.}
\State Initialize observed scores $\mathcal S\gets\varnothing$, $L\gets-\infty$, $U\gets+\infty$
\For{each checkpoint $t\in\mathcal T$}
\State Query only the new labels needed to reach $t$ observations and add their true-label scores to $\mathcal S$
\State Obtain the current rank bracket $(L_t,U_t)$ from the ordered observed scores
\State Form $(L',U')\gets(\max\{L,L_t\},\min\{U,U_t\})$
\If{$L'>U'$}
\State Record an empty-intersection event and use $(L',U')\gets(L_t,U_t)$
\EndIf
\State Set $(L,U)\gets(L',U')$ and calculate $D_t$ from the monitoring scores using Eq.~\eqref{eq:width}
\If{$D_t\le\tau$}
\State \Return queried count $t$ and predictor $C(x;U)$
\EndIf
\EndFor
\end{algorithmic}
\end{algorithm*}

\subsection{Finite-population rank brackets}
Choose checkpoints $\mathcal T\subseteq\{1,\ldots,N\}$ with $N\in\mathcal T$. If scores tie, order the complete population by the pair $(S_i,i)$, using the fixed pool index to break ties. The numerical inequalities below remain valid when these pairs are projected onto their score values.

Let $J_t$ count the queried items whose full-population ranks are strictly below $k$, and let $R_t$ indicate whether the item of rank $k$ has appeared. Then $J_t+R_t$ counts sampled items with rank at most $k$. Their separate marginal distributions are
\begin{equation}
 \begin{aligned}
 J_t&\sim\operatorname{Hypergeom}(N,k-1,t),\\
 J_t+R_t&\sim\operatorname{Hypergeom}(N,k,t).
 \end{aligned}
 \label{eq:hg}
\end{equation}
Write $Q_p(N,a,t)$ for the smallest integer whose hypergeometric cumulative probability is at least $p$. At a common tail parameter $\eta$, use sample-rank boundaries
\begin{equation}
 \ell_t=Q_\eta(N,k,t),\quad
 u_t=Q_{1-\eta}(N,k-1,t)+1.
 \label{eq:ranks}
\end{equation}
For the ordered observed scores $S^{[t]}_{(j)}$, set
\begin{equation}
 L_t=S_{(\ell_t)}^{[t]},\qquad U_t=S_{(u_t)}^{[t]}.
 \label{eq:bracket}
\end{equation}
Here $\ell_t$ is the lower sample-rank index defined in Eq.~\eqref{eq:ranks}. The conventions are $S^{[t]}_{(0)}=-\infty$ and $S^{[t]}_{(t+1)}=+\infty$. At the census, both bounds are the known $S_{(k)}$. When $k>N$, both are $+\infty$ and the reference already contains every label.

On a path satisfying $J_t+R_t\ge\ell_t$ and $J_t<u_t$, at least $\ell_t$ observations have full rank at most $k$, while fewer than $u_t$ observations have rank below $k$. Consequently, $L_t\le q_{\rm ref}\le U_t$. A standard simultaneous-coverage construction uses $\eta=\delta/[2(|\mathcal T|-1)]$ and a union bound over the non-census checkpoints. The resulting Bonferroni hypergeometric (HG) brackets form the first baseline.

\subsection{Joint calibration of the checkpoint boundaries}\label{sec:joint}
The union bound does not use the strong dependence between nearby checkpoints or between the two rank-count processes. We instead compute their joint crossing probability. Conditional on state $(J_t,R_t)=(j,r)$, the next observation has rank below $k$, equal to $k$, or above $k$, with probabilities
\begin{equation}
 \frac{k-1-j}{N-t},\qquad
 \frac{1-r}{N-t},\qquad
 \frac{N-k-t+j+r}{N-t},
 \label{eq:transition}
\end{equation}
respectively. They lead to states $(j+1,r)$, $(j,1)$ and $(j,r)$. Impossible transitions have probability zero. Start with mass one at $(0,0)$; propagate these probabilities and absorb mass at any checkpoint for which
\begin{equation}
 j+r<\ell_t\quad\text{or}\quad j\ge u_t.
 \label{eq:crossing}
\end{equation}
The absorbed mass $A(\eta)$ is the exact probability of at least one rank-boundary crossing under the finite query design. There are at most $2k$ states at each time. Direct propagation costs $O(Nk)$ operations and $O(k)$ working memory for a specified boundary family. It is performed before label queries and reused across pools with the same $N$, $k$, confidence level and checkpoints.

Increasing $\eta$ weakens both tail protections, so $A(\eta)$ is nondecreasing, although discrete. We search this one-dimensional family by 24 bisection steps and retain a feasible value satisfying $A(\eta)\le\delta-10^{-10}$. The small numerical slack is implementation protection; the statistical claim concerns the exact recursion. Exact group-sequential calibration is established \cite{jin}; the contribution here is the coupled rank-state construction for preserving a finite calibration reference, together with the prediction-set stopping target.

\subsection{Stopping in prediction-set space}
Intersect valid past brackets by setting $\overline L_t=\max_{v\le t}L_v$ and $\overline U_t=\min_{v\le t}U_v$, where $v\in\mathcal T$. On the simultaneous coverage event this intersection contains $q_{\rm ref}$. An empty intersection signals that event has failed; the implementation records it and falls back to the current bracket. At the census it returns the exact quantile regardless of previous events.

The stopping statistic is observable without monitoring labels:
\begin{equation}
 D_t=\frac1M\sum_{x\in\mathcal X_\star}
 \bigl[|C(x;\overline U_t)|-|C(x;\overline L_t)|\bigr].
 \label{eq:width}
\end{equation}
For a prespecified tolerance $\tau\ge0$, stop at the first checkpoint with $D_t\le\tau$ and return $C(x;\overline U_T)$. The census guarantees termination. The quantity $\tau$ is an absolute number of additional labels per input, not a normalized score width or coverage margin. An arbitrary numerical score tolerance would change meaning under a monotone reparameterization of the nonconformity score; this stopping statistic depends on actual prediction-set memberships.

\textbf{Proposition 1 (reference preservation).}
For a fixed classifier, finite reference pool, monitoring batch and declared checkpoints, suppose the joint recursion yields $A(\eta)\le\delta$. Under uniform sampling without replacement, with probability at least $1-\delta$, the returned predictor satisfies simultaneously
\begin{equation}
 C(x;q_{\rm ref})\subseteq C(x;\overline U_T),
 \label{eq:containment}
\end{equation}
for every input $x$, and
\begin{equation}
 \frac1M\sum_{x\in\mathcal X_\star}
 \bigl[|C(x;\overline U_T)|-|C(x;q_{\rm ref})|\bigr]
 \le\tau.
 \label{eq:inflation}
\end{equation}
No independence assumption on the fixed score values is required.

\textit{Proof.} The recursion enumerates the law of the uniformly sampled rank path. Except on an event of probability $A(\eta)$, neither inequality in Eq.~\eqref{eq:crossing} holds at any checkpoint. The rank argument above gives $L_t\le q_{\rm ref}\le U_t$ simultaneously. Running intersections therefore contain $q_{\rm ref}$ and are nonempty. Since the sets in Eq.~\eqref{eq:sets} are nested in $q$, the upper endpoint gives Eq.~\eqref{eq:containment} for every input. The reference set lies between the sets at the lower and upper endpoints, so its extra-label difference is bounded pointwise by the summand in Eq.~\eqref{eq:width}. At the data-dependent stopping time, the average is at most $\tau$. The confidence event covers every checkpoint and hence also this stopping time.\hfill$\square$

The containment event implies that empirical coverage cannot decrease relative to the same reference on any fixed labeled test collection. The confidence level is attached to one label-query campaign under the declared sampling design.

Strictly increasing score transformations preserve the rank ordering and every thresholded set membership, so they leave the query count and returned sets unchanged. This invariance provides an implementation check.

\subsection{Implementation and comparison methods}
Sorted monitoring scores allow Eq.~\eqref{eq:width} to be computed using two binary searches. Each checkpoint sorts the observed label scores. This direct implementation is adequate for the pool sizes studied; dynamic order-statistic structures could reduce repeated sorting for larger pools. Precomputed boundary tables are separate from per-pool query execution.

Both quantile-interval baselines satisfy the same finite-pool confidence constraint as JRC: conditional on the complete fixed pool, their intervals contain $q_{\rm ref}$ simultaneously at all declared checkpoints with probability at least $1-\delta$ over uniform sampling without replacement. The comparison uses the same upper-endpoint output and set-inflation stopping rule. Population coverage remains a separate property of the reference predictor.

The second quantile-interval baseline inverts the prior-posterior ratio confidence sequence (CS) of Waudby-Smith and Ramdas \cite{wor}. A uniform prior on the number of successes in the population induces the ordered-prefix mixture likelihood $B(j+1,t-j+1)$. For a hypothesized population count $a$, divide this mixture likelihood by
\begin{equation}
 \mathcal L_t(a,j)=
 \frac{(a)_j\,(N-a)_{t-j}}{(N)_t},
 \label{eq:likelihood}
\end{equation}
where $(a)_j$ denotes a falling factorial. Invert the resulting e-process separately at counts $k$ and $k-1$, allotting each error $\delta/2$. The minimum accepted count at $k$ gives the lower sample rank; one plus the maximum accepted count at $k-1$ gives the upper rank. We use the same running intersection, checkpoints and prediction-set stopping tolerance as the proposed engine.

We additionally compare ordinary conformal quantiles from fixed 25\%, 50\% and 75\% prefixes, and an upper hypergeometric bound at a single fixed 50\% look. The former have no reference-containment guarantee. The latter protects the reference at that fixed look but does not adapt label expenditure to a prescribed set-size tolerance. Diagnostic ablations omit time correction, omit running intersection, return a midpoint at the proposed stopping time, or use sparse checkpoints. Their different guarantees are kept explicit.
The one-look HG comparator queries $\lceil N/2\rceil$ labels and returns the upper endpoint of the same two-sided hypergeometric interval, with error 0.025 in each tail. Thus, ``50\% look'' identifies the query fraction. This comparator uses a more conservative upper endpoint than a one-sided bound assigning error 0.05 to the upper tail.

\section{Experimental design}
\label{sec:experiments}
\subsection{Data and development history}
The main evaluation includes six development datasets: UCI HAR \cite{har}, DSADS \cite{dsads}, MHEALTH \cite{mhealth}, WISDM \cite{wisdm}, PAMAP2 \cite{pamap} and REALDISP \cite{realdisp}. They contain 122 eligible participant identifiers and 54,482 retained windows. These datasets were examined during acquisition-policy development. Earlier risk-based and randomized-allocation rules showed poor tolerance transfer on PAMAP2 and REALDISP, motivating the fixed-reference target. Their protocols and aggregate negative results are retained in Online Resource 1.

USC-HAD \cite{uschad} provides the main confirmation benchmark. The method, confidence level, primary tolerance, query schedule, preprocessing and splits were locally locked on 12 September 2026 before model predictions were available. All 14 participants meet the requirement of at least 120 retained windows, yielding 5,353 windows and 12 classes. The main seven-dataset study therefore contains 136 dataset-specific participant identifiers and 59,835 windows.

The selected input is three acceleration axes resampled to 128 time points. UCI HAR uses the release's 2.56-second total-acceleration windows, retaining alternating windows per subject; its release ordering is not verified raw chronology. DSADS uses five-second torso-acceleration segments. MHEALTH uses five-second chest windows; WISDM uses five-second smartphone-acceleration windows from its 18-activity release. PAMAP2 uses protocol chest acceleration, and REALDISP uses ideal-placement BACK acceleration. The original REALDISP manual places BACK third in the log, unlike the fifth position listed in the repository webpage summary; we follow the original manual. The retained sample counts and preprocessing provenance are included in Table~\ref{tab:data} and the supplement. Different window durations in the original HAR release prevent interpreting the seven datasets as identical sensor protocols.

For USC-HAD, each original MAT trial remains separate. We take nonoverlapping 500-sample windows at the documented 100 Hz, retain acceleration columns 1--3, reject nonfinite windows and incomplete tails, and use anti-aliased polyphase resampling to 128 points. Dataset-provided trial segmentation is treated as available; our label-cost model does not charge or solve raw-stream segmentation. The filename convention supplies activity IDs. An independent schema audit finds one conflict: Subject14/a3t2.mat is activity 3 by filename but activity 2 (walking-left) in its internal metadata. Main labels follow the locked filename rule. A separate sensitivity excludes that trial, refitting classifiers whenever it occurs in training. Another file lacks the optional numeric activity field but has a matching activity-name string; it is retained. No exact duplicate signal arrays are found among the 840 trials. The main analysis and the exclusion sensitivity are both reported.


\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{3pt}
\caption{Public datasets and retained samples}\label{tab:data}
\begin{tabular}{lrrrlr}
\toprule
Dataset & People & Windows & Classes & Selected signal & Seconds \\
\midrule
UCI HAR & 30 & 5,155 & 6 & Waist total acceleration & 2.56 \\
DSADS & 8 & 9,120 & 19 & Torso acceleration & 5 \\
MHEALTH & 10 & 1,335 & 12 & Chest acceleration & 5 \\
WISDM & 51 & 32,653 & 18 & Phone acceleration & 5 \\
PAMAP2 & 8 & 3,775 & 12 & Chest acceleration & 5 \\
REALDISP & 15 & 2,444 & 33 & BACK acceleration & 5 \\
USC-HAD & 14 & 5,353 & 12 & Front right hip acceleration & 5 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize The first six datasets are development and reanalysis; USC-HAD is the locked confirmation benchmark. All signals have three axes and are resampled to 128 points. Eligibility requires at least 120 windows per participant. Sample counts refer to the fixed main preprocessing, before the separate USC-HAD schema sensitivity.\end{minipage}
\end{table*}

\subsection{Frozen classifiers and subject partitions}
Every acquisition method uses identical probability vectors within a classifier case. Logistic regression (LR) uses 35 standardized statistical features, regularization $C=1$ and a maximum of 2,000 iterations. MiniRocket (MR) uses 2,016 kernels followed by a standardized logistic-regression head. The InceptionTime-style model (IT) uses six modules, 32 filters per branch, kernel lengths 39, 19 and 9, residual connections every third module, global pooling and a classification head. It is trained for 40 epochs with Adam, learning rate $10^{-3}$ and batches of 128, without a test-selected checkpoint. The statistical features, normalization and model code are shared with the preceding frozen model study. Scikit-learn and SciPy support the classical pipeline \cite{sklearn,scipy}.

We reuse 249 cases from 83 outer partitions on the six development datasets. Each case has disjoint training, calibration and test participants. The earlier study trained auxiliary out-of-fold models, but this method does not require their predictions or their fit cost. USC-HAD has five fixed random seeds and seven test folds per seed: two test participants, eight training participants and four calibration participants in each fold. This gives 35 partitions and 105 new final-classifier fits. The 354 model cases reuse the 83 development and 35 confirmation partitions; repeated results are aggregated within participants.

The least ambiguous set-valued classifier (LAC) score \cite{sadinle} is $s(x,y)=1-\widehat p_y(x)$. The secondary score uses adaptive prediction sets (APS) \cite{romano} in a deterministic inclusive form: sort class probabilities in descending order, with stable class-index ties, and assign each class its cumulative probability. There is no randomization within the APS score and no forced top-label insertion. Empty and full sets remain in the calculations. The same scoring convention is used for the reference and every acquisition comparator, and $\alpha=0.1$ throughout.

\subsection{Reference pools and query experiments}
\noindent We construct each reference pool from $m$ calibration participants. The requested ceilings are $B\in\{120,240,480,960\}$, and the actual pool size is $N=\min(B,120m)$. Participant quotas differ by at most one; a random participant ordering allocates any remainder. Each quota is sampled uniformly without using its labels, after which the complete selected pool is independently permuted. When several ceilings produce the same pool size, the evaluation retains the declared configurations and reports their actual $N$.

The primary settings are confidence error $\delta=0.05$, mean set-inflation tolerance $\tau=0.5$, and checkpoints every 20 queried labels plus the census. The tolerance is measured in additional candidate labels per monitoring input. Secondary tolerances are 0.1, 0.25 and 1.0. Forty paired randomizations of pool selection and query order are used for each classifier case and ceiling. Every comparator shares the seeds, participants and windows. Complete labels define the evaluation reference; an oracle-based replay checks that acquisition decisions use only the labels already queried.

The complete test-input batch supplies monitoring scores with true labels hidden. The stopping statistic averages set size over windows in that batch. Empirical coverage and set size are also summarized with equal weight per participant. These two aggregations are retained explicitly in the result tables.

\subsection{Metrics, uncertainty and ablations}
Primary outcomes are queried-label savings $1-T/N$, reference-set containment failure and batch size inflation. A containment failure is the conservative scalar event $\overline U_T<q_{\rm ref}$; with ties or gaps in candidate scores, this can flag a threshold failure even when no observed test set loses a label. Bracket failure checks both endpoints. Size failure checks whether actual mean extra labels on the monitoring batch exceed $\tau$. Each query campaign is counted once, regardless of the number of held-out people.

For a test participant with empirical coverage $c_u$, positive shortfall is $(0.9-c_u)_+$. The positive part is calculated before averaging randomizations. Accuracy and macro-F1 describe the frozen point classifiers. Subject summaries average paired randomizations and repeated partitions; paired 95\% bootstrap intervals resample these summaries within each dataset. The intervals therefore describe participant variation conditional on the fitted models and partitions.

JRC, Bonferroni HG and the confidence sequence based on the prior-posterior ratio use identical tolerances and query checkpoints. Ablations remove the running intersection, use a sparse checkpoint grid, omit repeated-look correction or return the midpoint at the proposed stopping time. The uncorrected and midpoint variants serve as diagnostic controls. Fixed fractions and a one-look HG rule provide additional label-cost and set-size trade-offs. Exploratory checks vary $\delta$ and compare monitoring on even-indexed inputs with evaluation on odd-indexed inputs in the first MR case of each development dataset.

All runs use an Intel Core i9-12900HX CPU and an RTX 4070 Laptop GPU with 8 GB memory for neural training. We measure boundary-table construction separately from online acquisition calculations. Human annotation duration and device energy were outside the computational benchmark. Exhaustive small-population enumeration checks both rank engines and the coupled recursion; additional checks cover ties, census behavior, oracle access and strictly monotone score transformations.



\subsection{Additional HHAR confirmation}
After the main seven-dataset study, an additional Heterogeneity Human Activity Recognition (HHAR) confirmation \cite{hhar} was specified and publicly committed on 14 September 2026 before preprocessing outcomes or model fitting. The frozen method and classifier implementations were retained. All smartphone-accelerometer streams were processed separately by user and device. Five-second nonoverlapping windows were formed within activity-consistent runs, splitting at non-increasing timestamps or gaps above 0.5~s. Windows required at least 60 finite samples and 4.9~s of observed duration. Timestamp interpolation at 200~Hz followed by anti-aliased resampling produced 128 points. A label-blind random cap of 1,200 windows per user retained 10,800 windows from nine users and six classes. The fixed continuity filter retained very few Nexus4 windows. Device-specific retention counts are provided in Online Resource~1.

Five declared seeds and three user-disjoint test folds used four training, two calibration and three test users per fold, yielding 45 additional LR, MR and IT cases. The primary analysis used LAC, $N=240$, $\delta=0.05$, $\tau=0.5$ and 40 paired query orders; $N=120$ and APS were secondary. Alongside Bonferroni HG, the fixed Beta$(9,1)$ confidence-sequence prior was a primary comparator, chosen from the preceding diagnostic before HHAR outcomes. User-level paired differences and 95\% bootstrap intervals follow the declared aggregation. This cohort is reported separately from the main USC-HAD confirmation.

\section{Results}
\subsection{Label savings against sequential quantile baselines}
Table~\ref{tab:primary} reports absolute reference utility and label savings for the three interval methods. Figure~\ref{fig:2} adds the secondary APS score. All engines use the same confidence error, tolerance, pool and query order. This common setting isolates the acquisition efficiency of their quantile bounds.

\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{3pt}
\caption{Primary LAC results at confidence error 0.05 and tolerance 0.5}\label{tab:primary}
\begin{tabular}{lrrrrrr}
\toprule
Dataset & Ref. cov. (\%) & Ref. size & Joint saved (\%) & HG saved (\%) & CS saved (\%) & Joint extra size \\
\midrule
UCI HAR & 90.45 & 1.035 & 71.62 & 64.93 & 58.97 & 0.235 \\
DSADS & 88.69 & 5.015 & 4.19 & 2.71 & 1.10 & 0.073 \\
MHEALTH & 89.07 & 1.459 & 45.17 & 38.25 & 30.92 & 0.206 \\
WISDM & 89.92 & 8.996 & 6.59 & 4.35 & 2.49 & 0.122 \\
PAMAP2 & 87.14 & 2.933 & 16.20 & 12.02 & 6.77 & 0.151 \\
REALDISP & 89.80 & 2.377 & 18.56 & 13.77 & 8.82 & 0.163 \\
USC-HAD & 90.36 & 2.590 & 23.67 & 17.91 & 11.74 & 0.179 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize Saved (\%) = $100(1-T/N)$: window labels saved relative to the full reference pool of $N$ labels, excluding training, segmentation and setup costs. HG: Bonferroni hypergeometric brackets. CS: prior-posterior ratio rank confidence sequence. Coverage and set size give equal weight to participant summaries. Extra size is relative to the full reference. The stopping guarantee concerns the window-weighted monitoring batch.\end{minipage}
\end{table*}

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig2.pdf}}
\caption{Main-study reference-label savings: (a) LAC; (b) APS, at confidence error 0.05 and tolerance 0.5. Within each dataset, bars are ordered JRC, Bonferroni HG and the primary uniform-prior confidence sequence from left to right. Both panels use the same zero-based scale. Values average the declared pool ceilings and participant summaries; paired uncertainty is reported in Section~\ref{sec:paired}}\label{fig:2}
\end{figure*}
On USC-HAD, the joint engine saves 23.67\% of reference labels, compared with 17.91\% for HG and 11.74\% for the uniform-prior CS. Its mean coverage is 91.80\%, against 90.36\% for the reference, and mean set size changes from 2.590 to 2.769. The coverage increase comes from the more inclusive calibration threshold; the point classifiers are unchanged.

The six development datasets show a substantial range. Joint-engine savings are 71.62\% on HAR and 45.17\% on MHEALTH, but only 4.19\% on DSADS and 6.59\% on WISDM. PAMAP2 and REALDISP lie between these extremes. Large absolute savings are therefore not a universal property of certified stopping. Broad reference sets and score distributions with many decision changes near the reference quantile can make the tolerance costly to certify.
\subsection{Reference preservation and absolute utility}
Table~\ref{tab:confirm} and Fig.~\ref{fig:3} compare fixed-fraction reductions with the quantile-interval baselines. Fixed fractions provide substantial label savings by construction, but their ordinary prefix quantiles need not contain the full-reference sets. A midpoint at a certified stopping time similarly discards the reason that upper-endpoint containment holds. The one-look HG rule returns the upper endpoint of a two-sided interval at the predetermined 50\% query fraction; it does not impose the adaptive set-inflation tolerance.

\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{3pt}
\caption{USC-HAD primary results and run-level failures}\label{tab:confirm}
\begin{tabular}{lrrrrrr}
\toprule
Policy & Saved (\%) & Coverage (\%) & Set size & Window extra & Threshold fail (\%) & Size fail (\%) \\
\midrule
Full reference & 0.00 & 90.36 & 2.590 & 0.0000 & 0.00 & 0.00 \\
Joint rank engine & 23.67 & 91.80 & 2.769 & 0.1824 & 2.04 & 0.72 \\
Bonferroni HG & 17.91 & 91.86 & 2.774 & 0.1870 & 0.45 & 0.21 \\
Uniform-prior CS & 11.74 & 91.76 & 2.761 & 0.1735 & 0.02 & 0.05 \\
Fixed 25\% & 75.00 & 90.46 & 2.713 & 0.1220 & 42.26 & 15.03 \\
Fixed 50\% & 50.00 & 90.41 & 2.632 & 0.0416 & 41.42 & 6.09 \\
Fixed 75\% & 25.00 & 90.36 & 2.601 & 0.0102 & 39.83 & 2.20 \\
One-look HG (50\% look) & 50.00 & 93.83 & 3.356 & 0.7737 & 1.73 & 46.64 \\
Midpoint output & 23.67 & 90.04 & 2.562 & -0.0282 & 46.40 & 0.14 \\
No time correction & 33.71 & 91.73 & 2.766 & 0.1784 & 6.29 & 3.02 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize Saved (\%) = $100(1-T/N)$: window labels saved relative to the full reference pool of $N$ labels, excluding training, segmentation and setup costs. Coverage and set size use participant weighting. Window extra is mean set inflation on the monitoring windows, as in Eq.~(11), averaged equally over the declared query campaigns. One-look HG uses 2.5\% per tail and returns only the upper endpoint. Threshold failure is upper output threshold below the complete-reference threshold. Size failure is monitoring-batch inflation above 0.5 labels. Rates average paired query campaigns, each counted once. Fixed fractions, midpoint output and the uncorrected rule do not have the proposed joint contract.\end{minipage}
\end{table*}

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig3.pdf}}
\caption{USC-HAD LAC failure diagnostics: (a) output threshold below the full-reference threshold; (b) monitoring-batch inflation above 0.5 labels. The dashed line is $\delta=5\%$. The first three bars are the interval methods; gray bars are diagnostic rules without the same guarantee. The labels 25\%, 50\% and 75\% denote fractions of reference labels queried; Mid is midpoint output and Uncorr. is uncorrected repeated looks. Numerical values are reported in Table~\ref{tab:confirm}}\label{fig:3}
\end{figure*}
The proposed confirmation threshold-failure rate is 2.04\%, and its batch size-failure rate is 0.72\%. The confidence statement applies to one query campaign. A failure of the scalar threshold bracket may not remove any label on the finite observed input batch if no candidate score lies in the gap.

Reference quality explains important differences across datasets. Some development references fall below nominal 90\% coverage, and WISDM returns broad candidate sets. These cases show where improving the recognition or calibration stage remains important.
\subsection{Paired improvements and classifier dependence}\label{sec:paired}
Figure~\ref{fig:4} reports paired participant bootstrap intervals for additional savings against the quantile-interval baselines. These intervals condition on the fitted models and split collection. Table~\ref{tab:models} separates point-classification performance from acquisition outcomes for each confirmation backbone. The acquisition policy does not retrain or select among these classifiers.

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig4.pdf}}
\caption{Additional LAC label savings of JRC relative to (a) Bonferroni hypergeometric brackets and (b) the primary uniform-prior confidence sequence. Points are paired participant means; bars are 95\% bootstrap intervals conditional on the fitted models and partitions}\label{fig:4}
\end{figure*}

\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{3pt}
\caption{USC-HAD point-classifier and acquisition results}\label{tab:models}
\begin{tabular}{lrrrrr}
\toprule
Classifier & Accuracy (\%) & Macro-F1 (\%) & Labels saved (\%) & Set coverage (\%) & Set size \\
\midrule
LR & 60.17 & 56.14 & 32.85 & 92.76 & 2.769 \\
MR & 68.04 & 65.40 & 23.31 & 92.82 & 2.573 \\
IT & 65.14 & 60.32 & 14.85 & 89.83 & 2.967 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize Each classifier has 35 main outer partitions. IT denotes a single InceptionTime-style network. Macro-F1 includes all 12 classes, with zero contribution for undefined class scores according to the supplied implementation.\end{minipage}
\end{table*}
\subsection{Tolerance, query checkpoints and mechanism ablations}
Figure~\ref{fig:5} reports all four declared tolerances. Relaxing the allowed average number of additional labels permits earlier stopping, as expected from Eq.~\eqref{eq:width}. The tolerance controls the permitted added ambiguity and the resulting cost-utility trade-off. The primary tolerance remains 0.5 on confirmation; it was not selected by maximizing the confirmation savings.

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig5.pdf}}
\caption{Tolerance-savings curves for JRC: (a) LAC; (b) APS. Distinct markers and line styles identify the datasets. All four tolerances were declared before USC-HAD results. Values are means; primary-tolerance paired uncertainty is reported in Fig.~\ref{fig:4}}\label{fig:5}
\end{figure*}
Table~\ref{tab:ablate} separates the mechanism choices. Removing running intersections reduces confirmation savings from 23.67\% to 21.10\%; sparse checkpoints reduce them to 16.84\%. Returning the midpoint retains the same label count but raises threshold failure to 46.40\%, illustrating why the protected upper endpoint is essential. Omitting repeated-look correction increases savings to 33.71\% and threshold failure to 6.29\%, above the declared 5\% error level.

\begin{table*}[tp]
\centering\small\setlength{\tabcolsep}{3pt}
\caption{USC-HAD mechanism ablations}\label{tab:ablate}
\begin{tabular}{lrrrr}
\toprule
Rule & Saved (\%) & Extra size & Threshold fail (\%) & Size fail (\%) \\
\midrule
Joint rank engine & 23.67 & 0.179 & 2.04 & 0.72 \\
No intersection & 21.10 & 0.178 & 0.67 & 0.06 \\
Sparse checkpoints & 16.84 & 0.079 & 1.77 & 0.36 \\
Midpoint output & 23.67 & -0.028 & 46.40 & 0.14 \\
No time correction & 33.71 & 0.175 & 6.29 & 3.02 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize Saved (\%) = $100(1-T/N)$: window labels saved relative to the full reference pool of $N$ labels, excluding training, segmentation and setup costs. Only the first three rows retain the declared guarantee with their respective boundaries and checkpoints. The midpoint has the same stopping labels as the joint engine but a different output rule. The uncorrected rule reuses fixed-time intervals at multiple looks.\end{minipage}
\end{table*}
Figure~\ref{fig:6} traces the first declared USC-HAD MR query campaign at a 480-window pool. This case is selected by its fixed index, not by searching for the largest gain. The census collapses all valid constructions to the same full-reference quantile. Before that point, the mapping from rank uncertainty to set membership determines when each engine meets the tolerance.

\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig6.pdf}}
\caption{First declared USC-HAD MR query run, LAC, 480 reference windows: (a) running upper quantile endpoints represented as the corresponding probability cutoff $1-\overline U_t$ on a logarithmic axis; (b) endpoint set-size difference with stopping locations marked. The reference quantile is approximately 0.999436. Zero cutoffs and infinite upper bounds are displayed at $10^{-16}$ in (a); this is only a display convention. The horizontal line in (b) is the 0.5-label tolerance}\label{fig:6}
\end{figure*}
Figure~\ref{fig:7} relates label savings to absolute reference set size and shows the corresponding reference and returned-set coverage pairs.



\subsection{Sensitivity to stronger confidence-sequence priors}
The primary confidence-sequence baseline uses a uniform prior on the finite-population success count. After the main confirmation, we evaluated four fixed beta-binomial priors across all 354 existing classifier cases. A Beta$(a,b)$ mixture replaces the ordered-prefix mixture likelihood by $B(j+a,t-j+b)/B(a,b)$ while retaining the same likelihood-ratio construction and error allocation. Exact rank-path checks verify the error bound for each finite design used. The diagnostic reuses the main models and queries after confirmation; the locked JRC rule is unchanged.

Table~\ref{tab:priors} reports all four prior choices. Tail-centered priors improve the baseline, showing that the uniform-prior comparison alone understates the performance of this method family. On USC-HAD, the strongest of the tested alternatives saves 15.33\% of labels, compared with 11.74\% for the primary uniform prior and 23.67\% for JRC. JRC retains the largest dataset-average savings among these choices on all seven datasets. The comparison covers the four specified priors and the declared benchmark designs. Reconstructing JRC and the uniform-prior results reproduces the frozen dataset averages within $10^{-12}$.



\subsection{Schema exclusion and computation}
The exclusion sensitivity removes the USC-HAD trial with conflicting activity identifiers from every data role. The original classifier is reused only when that trial was absent from its training set; otherwise it is refitted. Joint-engine label savings change from 23.67\% to 22.70\%, coverage from 91.80\% to 92.30\%, and mean set size from 2.769 to 2.814. This check measures sensitivity to the ambiguity; it does not establish which of the conflicting original labels is correct. The locked main data and outcomes remain intact.

Boundary tables depend only on the finite design and can be reused. Table~\ref{tab:runtime} separates table construction from execution with a cached table. Timings exclude JIT compilation, model inference and human labeling. The online benchmark uses a fixed synthetic score array and 1,000 unlabeled inputs with 12 candidate classes to isolate computation; this benchmark isolates computation from recognition quality.


\begin{figure*}[tp]\centering
\makebox[\textwidth][c]{\includegraphics[width=174mm]{Fig7.pdf}}
\caption{Reference utility and acquisition behavior for LAC: (a) label savings versus full-reference set size; (b) returned-set versus reference coverage. Each point represents one dataset-classifier mean; dataset identity is encoded by both marker shape and color. Each dataset contributes LR, MR and IT points, whose identities and values are supplied in the model-summary table of Online Resource 1. The diagonal indicates equal coverage}\label{fig:7}
\end{figure*}
\begin{table*}[tp]
\begin{minipage}{\textwidth}

\begin{minipage}{\textwidth}
\centering\small\setlength{\tabcolsep}{5pt}
\reviewtablecaption{Post-confirmation prior sensitivity: percentage of reference labels saved}\label{tab:priors}
\begin{tabular}{lrrrrr}
\toprule
Dataset & JRC & CS$(1,1)$ & CS$(0.5,0.5)$ & CS$(1.8,0.2)$ & CS$(9,1)$ \\
\midrule
UCI HAR & 71.62 & 58.97 & 60.85 & 66.89 & 64.84 \\
DSADS & 4.19 & 1.10 & 1.12 & 2.09 & 1.82 \\
MHEALTH & 45.17 & 30.92 & 32.06 & 36.58 & 36.51 \\
WISDM & 6.59 & 2.49 & 2.53 & 3.02 & 3.34 \\
PAMAP2 & 16.20 & 6.77 & 6.95 & 9.24 & 9.20 \\
REALDISP & 18.56 & 8.82 & 9.16 & 10.86 & 11.73 \\
USC-HAD & 23.67 & 11.74 & 12.01 & 14.22 & 15.33 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize LAC, confidence error 0.05, tolerance 0.5 and the original paired query orders. CS$(a,b)$ uses a Beta$(a,b)$ mixing distribution. Values give equal weight to participant summaries. All four choices are reported; this diagnostic follows the main confirmation\end{minipage}
\end{minipage}
\par\vspace{16pt}\noindent
\begin{minipage}{\textwidth}
\centering\small\setlength{\tabcolsep}{3pt}
\reviewtablecaption{Boundary construction and cached acquisition computation}\label{tab:runtime}
\begin{tabular}{lrrr}
\toprule
Pool size & Build table (ms) & Cached query loop (ms) & Exact crossing probability \\
\midrule
120 & 8.31 & 0.18 & 0.04843 \\
240 & 25.07 & 0.26 & 0.04606 \\
480 & 53.80 & 0.50 & 0.04991 \\
960 & 153.41 & 0.45 & 0.04981 \\
\botrule
\end{tabular}
\begin{minipage}{\textwidth}\footnotesize Five repeated timings per size on the stated CPU. All confidence errors are 0.05. Boundary construction is a one-time cost for each distinct pool and checkpoint design; query execution includes sorting monitoring scores and observed prefixes.\end{minipage}
\end{minipage}
\par\vspace{16pt}\end{minipage}
\par\vspace{12pt}\begin{minipage}{\textwidth}
\centering\small\setlength{\tabcolsep}{2pt}
\reviewtablecaption{Additional HHAR primary confirmation, $N=240$}\label{tab:hhar}
\begin{tabular}{lrrr}
\toprule
Policy & Saved (\%) & Threshold & Inflation \\
 & & fail (\%) & fail (\%) \\
\midrule
Reference & 0.00 & 0.00 & 0.00 \\
JRC & 34.03 & 1.33 & 0.28 \\
HG & 28.58 & 0.39 & 0.00 \\
CS$(9,1)$ & 25.13 & 0.28 & 0.00 \\
CS$(1,1)$ & 20.64 & 0.06 & 0.00 \\
\botrule
\end{tabular}
\par\vspace{3pt}\raggedright\footnotesize Saved (\%) = $100(1-T/N)$ relative to the complete reference pool, excluding training, segmentation and setup costs. CS$(a,b)$ uses a Beta$(a,b)$ prior; CS$(1,1)$ is uniform. Failure rates count query campaigns once.\par
\end{minipage}
\end{table*}
\subsection{Additional HHAR confirmation}
Table~\ref{tab:hhar} reports the additional primary confirmation. JRC saves 34.03\% of reference labels, compared with 28.58\% for HG and 25.13\% for the pre-specified Beta$(9,1)$ CS. The paired differences are 5.45 percentage points (95\% CI 5.09 to 5.85) against HG and 8.89 percentage points (95\% CI 8.52 to 9.23) against the stronger-prior CS. These intervals describe participant variation conditional on the fitted models and partitions.

The full reference has mean coverage 88.10\% and mean set size 1.807; JRC returns 90.84\% coverage and mean set size 1.997. Threshold and monitoring-batch inflation failures are reported alongside savings. Secondary pool-size, APS and classifier-specific results are retained in Online Resource~1. This additional cohort supports the acquisition gain on a separately collected dataset under the fixed continuity rules.



\clearpage\onecolumn\twocolumn
\section{Discussion and limitations}
JRC improves label efficiency by calibrating the joint rank path across checkpoints. The benefit is greatest when the reference quantile can be localized while adding few candidate labels. The main experiments and the separately specified HHAR confirmation support this mechanism. Stronger confidence-sequence priors recover part of the gap to the uniform-prior baseline, while JRC retains an advantage in the reported dataset averages.

The reference predictor determines the practical value of this preservation guarantee. Broad prediction sets or weak reference coverage call for improvements to recognition and calibration. The containment statement applies to every input, while the size-inflation bound concerns the specified monitoring batch. The confidence-level checks and the comparison of even- and odd-indexed monitoring inputs in Online Resource 1 characterize sensitivity to these choices.

The evaluation combines six development datasets with the main USC-HAD confirmation and a separately specified HHAR confirmation. The fixed HHAR continuity rule leaves very few Nexus4 windows, so its evidence concerns the retained records. Further deployment conditions and practical annotation studies would extend this evidence. In particular, scattered window queries can incur context-switching costs: the modeled effort $T+cm$ captures participant setup but omits human annotation duration and segmentation effort. Extending the sampling law to clustered or nonuniform queries would support those workflows.

\section{Conclusion}
Joint rank calibration reduces the labels required to preserve a fixed calibration reference. Its coupled recursion controls quantile-boundary crossings across the declared checkpoints, and its stopping rule measures the resulting prediction-set ambiguity on unlabeled inputs. The main evaluation and the additional HHAR confirmation show useful savings over the tested interval comparators. The results also identify low-saving cases and the importance of reference quality, providing a concrete basis for choosing when to apply the method.


\backmatter
\section*{Data availability}
The eight public datasets analyzed in this study are available from their original sources \cite{har,dsads,mhealth,wisdm,pamap,realdisp,uschad,hhar}. Frozen classifier predictions, evaluation tables and protocol records supporting the reported results are provided in Online Resource~1.

\section*{Code availability}
The acquisition algorithms, analysis code and reproduction instructions are available at \url{https://github.com/Cyrano666/jrc-reference-emulation}. Online Resource~1 contains the accompanying code snapshot.

\section*{Supplementary information}
Online Resource~1: reproduction code, frozen predictions, evaluation tables, protocol records, verification results and editable figure sources.

\begin{thebibliography}{99}\fontsize{9}{11}\selectfont\setlength{\itemsep}{2pt}
\bibitem{guo}\begin{minipage}[t]{\linewidth}Guo C, Pleiss G, Sun Y, Weinberger KQ (2017) On calibration of modern neural networks. Proceedings of Machine Learning Research 70:1321-1330. \url{https://proceedings.mlr.press/v70/guo17a.html}\end{minipage}

\bibitem{ovadia}\begin{minipage}[t]{\linewidth}Ovadia Y, Fertig E, Ren J, Nado Z, Sculley D, Nowozin S, Dillon J, Lakshminarayanan B, Snoek J (2019) Can you trust your model's uncertainty? Evaluating predictive uncertainty under dataset shift. Advances in Neural Information Processing Systems 32. \url{https://proceedings.neurips.cc/paper/2019/hash/8558cb408c1d76621371888657d2eb1d-Abstract.html}\end{minipage}

\bibitem{vovk}\begin{minipage}[t]{\linewidth}Vovk V, Gammerman A, Shafer G (2005) Algorithmic learning in a random world. Springer, New York. \url{https://doi.org/10.1007/b106715}\end{minipage}

\bibitem{angelopoulos}\begin{minipage}[t]{\linewidth}Angelopoulos AN, Bates S (2023) Conformal prediction: A gentle introduction. Foundations and Trends in Machine Learning 16(4):494-591. \url{https://doi.org/10.1561/2200000101}\end{minipage}

\bibitem{dunn}\begin{minipage}[t]{\linewidth}Dunn R, Wasserman L, Ramdas A (2023) Distribution-free prediction sets for two-layer hierarchical models. Journal of the American Statistical Association 118(544):2491-2502. \url{https://doi.org/10.1080/01621459.2022.2060112}\end{minipage}

\bibitem{barber}\begin{minipage}[t]{\linewidth}Barber RF, Candes EJ, Ramdas A, Tibshirani RJ (2023) Conformal prediction beyond exchangeability. Annals of Statistics 51(2):816-845. \url{https://doi.org/10.1214/23-AOS2276}\end{minipage}

\bibitem{ilyas}\begin{minipage}[t]{\linewidth}Ilyas A, Ko J, Tang J, Wu S, Zhang J (2026) Provably label-efficient conformal prediction. International Conference on Machine Learning. \url{https://icml.cc/virtual/2026/poster/65796}\end{minipage}

\bibitem{howard2021}\begin{minipage}[t]{\linewidth}Howard SR, Ramdas A, McAuliffe J, Sekhon J (2021) Time-uniform, nonparametric, nonasymptotic confidence sequences. Annals of Statistics 49(2):1055--1080. \url{https://doi.org/10.1214/20-AOS1991}\end{minipage}

\bibitem{howard2022}\begin{minipage}[t]{\linewidth}Howard SR, Ramdas A (2022) Sequential estimation of quantiles with applications to A/B testing and best-arm identification. Bernoulli 28(3):1704--1728. \url{https://doi.org/10.3150/21-BEJ1388}\end{minipage}

\bibitem{wor}\begin{minipage}[t]{\linewidth}Waudby-Smith I, Ramdas A (2020) Confidence sequences for sampling without replacement. Advances in Neural Information Processing Systems 33. \url{https://proceedings.neurips.cc/paper_files/paper/2020/file/e96c7de8f6390b1e6c71556e4e0a4959-Paper.pdf}\end{minipage}

\bibitem{stfal}\begin{minipage}[t]{\linewidth}Huan R, Zhou Y, Dong G, Chen P, Liang R, Chen C (2026) STF-AL: spatiotemporal-frequency feature representation and adversarial learning for sensor-based cross-domain human activity recognition. Pattern Analysis and Applications 29:173. \url{https://doi.org/10.1007/s10044-026-01750-8}\end{minipage}

\bibitem{ccthar}\begin{minipage}[t]{\linewidth}Zhou X, Yuan J, Liu C, Chen Q (2026) CCT-HAR: Cross-modal contrastive sample mining with temporal alignment for self-supervised human action recognition. Pattern Analysis and Applications 29:44. \url{https://doi.org/10.1007/s10044-026-01628-9}\end{minipage}

\bibitem{tsdfsa}\begin{minipage}[t]{\linewidth}Zhang G, Dai G, Liu Z, Shi J, Lin Y (2025) TSDFSA: a two-stage dynamic feature selection method based on attention mechanism for multi-sensor data. Pattern Analysis and Applications 28:172. \url{https://doi.org/10.1007/s10044-025-01550-6}\end{minipage}

\bibitem{roy}\begin{minipage}[t]{\linewidth}Roy D, Girdzijauskas S, Socolovschi S (2021) Confidence-calibrated human activity recognition. Sensors 21(19):6566. \url{https://doi.org/10.3390/s21196566}\end{minipage}

\bibitem{hduq}\begin{minipage}[t]{\linewidth}Lamaakal I, Yahyati C, Maleh Y, El Makkaoui K, Ouahbi I (2026) Efficient uncertainty aware human activity recognition on microcontrollers using hyperdimensional computing and conformal prediction. Scientific Reports 16:26262. \url{https://doi.org/10.1038/s41598-026-57375-8}\end{minipage}

\bibitem{minirocket}\begin{minipage}[t]{\linewidth}Dempster A, Schmidt DF, Webb GI (2021) MiniRocket A very fast almost deterministic transform for time series classification. Proceedings of KDD, pp 248-257. \url{https://doi.org/10.1145/3447548.3467231}\end{minipage}

\bibitem{inception}\begin{minipage}[t]{\linewidth}Ismail Fawaz H, Lucas B, Forestier G, Pelletier C, Schmidt DF, Weber J, Webb GI, Idoumghar L, Muller PA, Petitjean F (2020) InceptionTime: Finding AlexNet for time series classification. Data Mining and Knowledge Discovery 34:1936-1962. \url{https://doi.org/10.1007/s10618-020-00710-y}\end{minipage}

\bibitem{vovk2012}\begin{minipage}[t]{\linewidth}Vovk V (2012) Conditional validity of inductive conformal predictors. Proceedings of Machine Learning Research 25:475--490. \url{https://proceedings.mlr.press/v25/vovk12.html}\end{minipage}

\bibitem{liangstop}\begin{minipage}[t]{\linewidth}Liang Z, Zhou Y, Sesia M (2023) Conformal inference is (almost) free for neural networks trained with early stopping. Proceedings of Machine Learning Research 202:20810--20851. \url{https://proceedings.mlr.press/v202/liang23i.html}\end{minipage}

\bibitem{active2024}\begin{minipage}[t]{\linewidth}Xu Z, Karampatziakis N, Mineiro P (2024) Active, anytime-valid risk controlling prediction sets. Advances in Neural Information Processing Systems 37:60110--60132. \url{https://doi.org/10.52202/079017-1920}\end{minipage}

\bibitem{cochran}\begin{minipage}[t]{\linewidth}Cochran WG (1977) Sampling techniques, 3rd edn. Wiley, New York. \url{https://www.wiley-vch.de/en/areas-interest/mathematics-statistics/sampling-techniques-978-0-471-16240-7}\end{minipage}

\bibitem{jin}\begin{minipage}[t]{\linewidth}Jin M, Kepner JL (2017) On sample size calculation for exact group sequential tests for rare disease. International Journal of Clinical Biostatistics and Biometrics 3:013. \url{https://doi.org/10.23937/2469-5831/1510013}\end{minipage}

\bibitem{romano}\begin{minipage}[t]{\linewidth}Romano Y, Sesia M, Candes EJ (2020) Classification with valid and adaptive coverage. Advances in Neural Information Processing Systems 33:3581-3591. \url{https://proceedings.neurips.cc/paper/2020/hash/244edd7e85dc81602b7615cd705545f5-Abstract.html}\end{minipage}

\bibitem{har}\begin{minipage}[t]{\linewidth}Reyes-Ortiz J, Anguita D (2013) Human activity recognition using smartphones. UCI Machine Learning Repository. Dataset. CC BY 4.0. \url{https://doi.org/10.24432/C54S4K}\end{minipage}

\bibitem{dsads}\begin{minipage}[t]{\linewidth}Barshan B, Altun K (2010) Daily and sports activities. UCI Machine Learning Repository. Dataset. CC BY 4.0. \url{https://doi.org/10.24432/C5C59F}\end{minipage}

\bibitem{mhealth}\begin{minipage}[t]{\linewidth}Banos O, Garcia R (2014) MHEALTH. UCI Machine Learning Repository. Dataset. \url{https://doi.org/10.24432/C5TW22}\end{minipage}

\bibitem{wisdm}\begin{minipage}[t]{\linewidth}Weiss G (2019) WISDM Smartphone and Smartwatch Activity and Biometrics Dataset. UCI Machine Learning Repository. Dataset. \url{https://doi.org/10.24432/C5HK59}\end{minipage}

\bibitem{pamap}\begin{minipage}[t]{\linewidth}Reiss A (2012) PAMAP2 Physical Activity Monitoring. UCI Machine Learning Repository. Dataset. \url{https://doi.org/10.24432/C5NW2H}\end{minipage}

\bibitem{realdisp}\begin{minipage}[t]{\linewidth}Banos O, Toth M, Amft O (2012) REALDISP Activity Recognition Dataset. UCI Machine Learning Repository. Dataset. \url{https://doi.org/10.24432/C5GP6D}\end{minipage}

\bibitem{uschad}\begin{minipage}[t]{\linewidth}Zhang M, Sawchuk AA (2012) USC-HAD: A daily activity dataset for ubiquitous activity recognition using wearable sensors. UbiComp Workshop on Situation, Activity and Goal Awareness. \url{https://sipi.usc.edu/had/mi_ubicomp_sagaware12.pdf}\end{minipage}

\bibitem{sklearn}\begin{minipage}[t]{\linewidth}Pedregosa F, Varoquaux G, Gramfort A, et al (2011) Scikit-learn Machine learning in Python. Journal of Machine Learning Research 12:2825-2830. \url{https://jmlr.org/papers/v12/pedregosa11a.html}\end{minipage}

\bibitem{scipy}\begin{minipage}[t]{\linewidth}Virtanen P, Gommers R, Oliphant TE, et al (2020) SciPy 1.0: fundamental algorithms for scientific computing in Python. Nature Methods 17:261-272. \url{https://doi.org/10.1038/s41592-019-0686-2}\end{minipage}

\bibitem{sadinle}\begin{minipage}[t]{\linewidth}Sadinle M, Lei J, Wasserman L (2019) Least ambiguous set-valued classifiers with bounded error levels. Journal of the American Statistical Association 114(525):223--234. \url{https://doi.org/10.1080/01621459.2017.1395341}\end{minipage}

\bibitem{hhar}\begin{minipage}[t]{\linewidth}Blunck H, Bhattacharya S, Prentow T, Kjaergaard M, Dey A (2015) Heterogeneity activity recognition. UCI Machine Learning Repository. Dataset. CC BY 4.0. \url{https://doi.org/10.24432/C5689X}\end{minipage}
\end{thebibliography}\end{document}

